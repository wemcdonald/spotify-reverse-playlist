# Spotify API Credentials
# Get these from https://developer.spotify.com/dashboard

# Your Spotify application's client ID
SPOTIFY_CLIENT_ID=your_client_id_here

# Your Spotify application's client secret
SPOTIFY_CLIENT_SECRET=your_client_secret_here

# Redirect URI for Spotify authentication (optional, defaults to http://localhost:8888/callback)
# SPOTIFY_REDIRECT_URI=http://localhost:8888/callback

# Custom path for storing the Spotify authentication token (optional)
# If not specified, the token will be stored in the script directory or ~/.config/spotify-reverse-playlist/
# SPOTIFY_TOKEN_PATH=/path/to/your/token/storage/.spotify-token.json

# Important: Make sure to add the redirect URI to your Spotify app settings 